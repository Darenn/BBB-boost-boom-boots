[gd_scene load_steps=2 format=2]

[sub_resource type="GDScript" id=1]
script/source = "extends Node2D


# Declare member variables here. Examples:
# var a: int = 2
# var b: String = \"text\"


# Called when the node enters the scene tree for the first time.
func _process(delta) -> void:
	SilentWolf.Scores.persist_score(\"test1\", 20)
	SilentWolf.Scores.persist_score(\"test2\", 20)
	SilentWolf.Scores.persist_score(\"test3\", 20)
	SilentWolf.Scores.persist_score(\"test4\", 20)
	SilentWolf.Scores.persist_score(\"test5\", 20)
	SilentWolf.Scores.persist_score(\"test6\", 20)
	SilentWolf.Scores.persist_score(\"test7\", 20)
	SilentWolf.Scores.persist_score(\"test8\", 20)


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta: float) -> void:
#	pass
"

[node name="Node2D" type="Node2D"]
script = SubResource( 1 )
